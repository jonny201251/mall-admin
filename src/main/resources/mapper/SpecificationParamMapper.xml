<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hthyaq.malladmin.mapper.SpecificationParamMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.hthyaq.malladmin.model.entity.SpecificationParam">
        <id column="id" property="id" />
        <result column="category_id" property="categoryId" />
        <result column="group_id" property="groupId" />
        <result column="name" property="name" />
        <result column="digit" property="digit" />
        <result column="unit" property="unit" />
        <result column="generic" property="generic" />
        <result column="searching" property="searching" />
        <result column="segments" property="segments" />
        <result column="sort" property="sort" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, category_id, group_id, name, digit, unit, generic, searching, segments, sort
    </sql>

</mapper>
